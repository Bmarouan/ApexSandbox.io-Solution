public String getAccountBillingCityWithSafeNavigation(List<Account> accounts){
   List<Account> acc = [ Select Id, BillingCity from Account ];

   String result = acc[0]?.BillingCity?. != null ? acc.BillingCity : 'No city';

   System.debug (result);
}

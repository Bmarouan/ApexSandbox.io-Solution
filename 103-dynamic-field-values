public List<String> getFieldsValue(Account acc, List<String> fields)
{
   List<String> fieldValues = new List<String>();
    
    for (String field : fields) {
        String value = acc.get(field);
        fieldValues.add(value);
    }
    
    return fieldValues;
}